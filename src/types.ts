export interface LiveShowDetails {
  schedule_date: string;
  schedule_time: string;
  show_name: string;
  product_name: string;
  product_category: string;
  notify_followers: boolean;
}

export type Language = 'Bangla' | 'English' | null;
